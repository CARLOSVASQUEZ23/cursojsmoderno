module.exports = {
  watch: true,
  interceptConsoleLogs: true,  // Esto habilita la captura de los logs de consola
  environments: ['node'],      // Establece el entorno en el que trabajas (por ejemplo, Node.js)
  logLevel: 'info',            // Define el nivel de los logs que quieres ver ('info', 'warn', 'error')
};
